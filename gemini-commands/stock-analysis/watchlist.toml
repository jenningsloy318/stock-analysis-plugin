prompt = """
<purpose>Review previously generated reports and provide status update. Spawns search-agent for current price data and catalyst checks.</purpose>

<usage>/stock-analysis:watchlist [TICKER|all]</usage>

<agent-team>
MANDATORY: This command operates as an agent team.

Delegate data gathering to sub-agent:

| Agent | Task |
|-------|------|
| @search-agent | Fetch current prices, news, catalyst updates for each watchlist ticker |

Orchestrator reads existing reports, delegates current data fetch, then compiles status.
</agent-team>

<process>
  <step n="1" name="Scan Reports (you do this)">Read ./reports/ directory for all existing analysis reports</step>
  <step n="2" name="Spawn Agent">Delegate to @search-agent for current price, news, and catalyst data for each ticker</step>
  <step n="3" name="Status Check (you do this)">Compare current data to bull/base/bear targets, check kill switches</step>
  <step n="4" name="Summary (you do this)">Output watchlist table with status indicators</step>
</process>

<constraints>
  <constraint>Only works if reports exist in ./reports/ from prior analyses</constraint>
  <constraint>Flag reports older than 90 days as "STALE"</constraint>
  <constraint>If kill switch condition approaching, highlight with warning</constraint>
</constraints>

User request: {{args}}
"""
