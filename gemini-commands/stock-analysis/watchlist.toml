prompt = """
<output-language>
CRITICAL: 所有报告必须使用中文撰写。ALL reports MUST be written in Chinese (中文).
DO NOT write reports in English under any circumstances.
Only technical terms (P/E, EV/EBITDA, ROIC, ticker symbols) and source citations may remain in English.
</output-language>

<purpose>Review previously generated reports and provide status update. Spawns search-agent for current price data and catalyst checks. Output in Chinese.</purpose>

<usage>/stock-analysis:watchlist [TICKER|all]</usage>

<agent-team>
MANDATORY: This command operates as an agent team.

STEP 0 — Read ./reports/ directory for existing reports. This is the only step you do directly.
STEP 1 — Spawn @search-agent for current data.

| Agent | Task |
|-------|------|
| @search-agent | Fetch current prices, news, catalyst updates for each watchlist ticker |

Orchestrator reads existing reports, delegates current data fetch, then compiles status.
</agent-team>

<process>
  <step n="0" name="Scan Reports">Read ./reports/ directory for all existing analysis reports. This is the only step you do directly.</step>
  <step n="1" name="Spawn Agent">Delegate to @search-agent for current price, news, and catalyst data for each ticker</step>
  <step n="2" name="Status Check">Compare current data to bull/base/bear targets, check kill switches</step>
  <step n="3" name="Summary">
    Output watchlist status table.
    LANGUAGE: 必须使用中文撰写。Begin output with:
    ```
    # 投资组合监控报告
    **更新日期**: [DATE]
    ## 持仓状态总览
    ```
    Write entire watchlist update in Chinese. Only tickers and metric names in English.
  </step>
</process>

<constraints>
  <constraint>ALL output MUST be written in Chinese (中文). Technical terms (ticker symbols, metric names) may remain in English.</constraint>
  <constraint>Only works if reports exist in ./reports/ from prior analyses</constraint>
  <constraint>Flag reports older than 90 days as "STALE"</constraint>
  <constraint>If kill switch condition approaching, highlight with warning</constraint>
</constraints>

User request: {{args}}
"""
