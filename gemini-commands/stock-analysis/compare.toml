prompt = """
<output-language>
CRITICAL: 所有报告必须使用中文撰写。ALL reports MUST be written in Chinese (中文).
DO NOT write reports in English under any circumstances.
Only technical terms (P/E, EV/EBITDA, ROIC, ticker symbols) and source citations may remain in English.
</output-language>

<purpose>Perform comparative analysis across 2-5 stocks using agent team. Spawns search-agent for data fetch, then quant-analyst for each ticker to produce consistent scoring, then generates comparison table in Chinese.</purpose>

<usage>/stock-analysis:compare [TICKER1],[TICKER2],[TICKER3]...</usage>

<agent-team>
MANDATORY: This command operates as an agent team. You MUST NOT run scripts directly.

STEP 0 — Confirm all tickers are valid, resolve ambiguous names. Do NOT run any scripts yourself.
STEP 1 — Spawn @search-agent for data fetch (fetch_financials.py, calculate_metrics.py for each ticker).
STEP 2 — Spawn scoring agents in parallel:

| Agent | Task |
|-------|------|
| @search-agent | Data fetch: financials and metrics for each ticker |
| @quant-analyst | Valuation and scoring for each ticker (run in parallel, 1 per ticker) |
| @fundamental-analyst | Financial health scoring for each ticker (run in parallel) |

Max 3 agents concurrent. Orchestrator merges scores into comparison table.
</agent-team>

<process>
  <step n="0" name="Team Setup">Confirm all tickers valid, resolve ambiguous names. This is the only step you do directly.</step>
  <step n="1" name="Data Fetch">Spawn @search-agent to run fetch_financials.py and calculate_metrics.py for each ticker. Wait for completion.</step>
  <step n="2" name="Spawn Agents">Delegate to @quant-analyst and @fundamental-analyst for each ticker in parallel</step>
  <step n="3" name="Comparison">
    Merge scores into side-by-side comparison table and ranking.
    LANGUAGE: 必须使用中文撰写。Begin output with:
    ```
    # 股票对比分析报告
    **分析日期**: [DATE]
    ## 综合评分对比
    ```
    Write entire comparison in Chinese. Only tickers and metric names in English.
  </step>
</process>

<constraints>
  <constraint>ALL output MUST be written in Chinese (中文). Technical terms (P/E, EV/EBITDA, ROIC, ticker symbols) may remain in English.</constraint>
  <constraint>NEVER run scripts or perform deep analysis directly — delegate to sub-agents for scoring</constraint>
  <constraint>Maximum 5 stocks per comparison</constraint>
  <constraint>All stocks should share GICS sector alignment</constraint>
  <constraint>Use identical valuation methodology across all stocks</constraint>
</constraints>

User request: {{args}}
"""
