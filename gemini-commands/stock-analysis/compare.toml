prompt = """
<purpose>Perform comparative analysis across 2-5 stocks using agent team. Spawns quant-analyst for each ticker to produce consistent scoring, then generates comparison table.</purpose>

<usage>/stock-analysis:compare [TICKER1],[TICKER2],[TICKER3]...</usage>

<agent-team>
MANDATORY: This command operates as an agent team.

After data fetch, delegate scoring to sub-agents:

| Agent | Task |
|-------|------|
| @quant-analyst | Valuation and scoring for each ticker (run in parallel, 1 per ticker) |
| @fundamental-analyst | Financial health scoring for each ticker (run in parallel) |

Max 3 agents concurrent. Orchestrator merges scores into comparison table.
</agent-team>

<process>
  <step n="1" name="Validate (you do this)">Confirm all tickers are valid, resolve ambiguous names</step>
  <step n="2" name="Data Fetch (you do this)">Run fetch_financials.py and calculate_metrics.py for each ticker</step>
  <step n="3" name="Spawn Agents">Delegate to @quant-analyst and @fundamental-analyst for each ticker in parallel</step>
  <step n="4" name="Comparison (you do this)">Merge scores into side-by-side comparison table, rank by composite</step>
</process>

<constraints>
  <constraint>NEVER perform deep analysis directly — delegate to sub-agents for scoring</constraint>
  <constraint>Maximum 5 stocks per comparison</constraint>
  <constraint>All stocks should share GICS sector alignment</constraint>
  <constraint>Use identical valuation methodology across all stocks</constraint>
</constraints>

User request: {{args}}
"""
