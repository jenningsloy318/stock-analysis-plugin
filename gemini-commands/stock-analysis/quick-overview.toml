prompt = """
<purpose>Perform a rapid stock analysis using the agent team in quick mode. Spawns fundamental-analyst, quant-analyst, and risk-analyst in parallel for condensed coverage. Produces 3 condensed reports (long/mid/short).</purpose>

<usage>/stock-analysis:quick-overview [TICKER]</usage>

<agent-team>
MANDATORY: This command operates as an agent team in quick mode.

After Triage, delegate to sub-agents using @agent-name syntax:

| Agent | Task |
|-------|------|
| @fundamental-analyst | Light fundamental analysis (key ratios, moat signal) |
| @quant-analyst | Valuation + technicals (DCF, comps, trend) |
| @risk-analyst | Key risks, Altman Z, red flags |

All 3 agents run in parallel, then scoring + report generation.
</agent-team>

<process>
  <step n="1" name="Triage (you do this)">Identify ticker, run fetch_financials.py, fetch_macro.py, calculate_metrics.py</step>
  <step n="2" name="Spawn Agents">Delegate to @fundamental-analyst, @quant-analyst, @risk-analyst in parallel</step>
  <step n="3" name="Scoring (you do this)">Run compute_scores.py, then spawn @equity-report-writer for 3 condensed reports</step>
</process>

<constraints>
  <constraint>NEVER perform analysis directly — always delegate to sub-agents</constraint>
  <constraint>Always produce 3 reports (long/mid/short) even in quick mode</constraint>
  <constraint>Estimated time: 2-5 minutes</constraint>
  <constraint>Report confidence flagged as "Limited — quick overview mode"</constraint>
</constraints>

User request: {{args}}
"""
